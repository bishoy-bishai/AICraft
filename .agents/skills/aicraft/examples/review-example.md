# Review Example

Bad review:

> I don't like this abstraction.

AICraft review:

> [MAJOR] This introduces a new persistence model even though the existing Resource primitive already represents the concept. This creates a second source of truth and can drift from the canonical contract. Prefer composing the existing Resource model.

The second comment is actionable because it is grounded in an architectural rule and explains the risk.
